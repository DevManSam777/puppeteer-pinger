# Discord webhook for notifications (optional)
# Get your webhook URL from Discord: Server Settings > Integrations > Webhooks
DISCORD_WEBHOOK=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Ping configuration
# Comma-separated list of URLs to keep alive
PING_URLS=http://localhost:3000, https://your-app-1.onrender.com, https://your-app-2.onrender.com

# How often to ping the apps (in minutes)
PING_INTERVAL_MIN=10

# How long to keep browser tabs open (in seconds) to ensure full spin-up
PAGE_WAIT_SEC=180

# Fallback URL if PING_URLS not set (auto-set by Render)
RENDER_EXTERNAL_URL=http://localhost:3000
